- `[x]` Secure the `vps1.pem` file permissions
- `[x]` Compress the `huevivu-trainer` project (excluding `node_modules` and `.next`)
- `[x]` Upload the archive to the AWS EC2 VPS via SCP
- `[ ]` SSH into the VPS and extract the project
- `[ ]` Run `npm install`, `npm run build`, and start PM2 on port 3003
- `[ ]` Run `node scripts/export_docs.js` in the main app
- `[ ]` Test the flow and write walkthrough
